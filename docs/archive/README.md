# Archive

`docs/archive/` stores historical material that is still useful for traceability but is no longer front-of-mind documentation.

Use these files for:

- implementation history
- superseded plans
- decision records that have already been absorbed into core docs

When an archived file disagrees with a root-level doc, the root-level doc wins.
