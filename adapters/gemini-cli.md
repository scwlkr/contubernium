# Gemini CLI Adapter

Gemini CLI integrations must concatenate shared runtime guidance, agent core files, selected actions, project context, and live state without allowing project-local behavior overrides.
